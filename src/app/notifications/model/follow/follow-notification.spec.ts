import { FollowNotification } from './follow-notification';

describe('FollowNotification', () => {
  it('should create an instance', () => {
    expect(new FollowNotification()).toBeTruthy();
  });
});
